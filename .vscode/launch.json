{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "gdb",
            "request": "attach",
            "name": "debug xv6",
            "executable": "./kernel",
            "target": "localhost:26000",
            "remote": true,
            "cwd": "${workspaceRoot}",
            "autorun": [
                "set substitute-path /root ."
              ]
        }
    ]
}