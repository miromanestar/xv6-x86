{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "gdb",
            "request": "attach",
            "name": "launch gdb xv6",
            "executable": "./kernel",
            "target": "localhost:26001",
            "remote": true,
            "cwd": "${workspaceRoot}",
            "autorun": [
                "set substitute-path /root ."
              ]
        },
    ]
}